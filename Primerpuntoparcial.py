def primosLejanos():
    x = int(input("Digite un número natural que sea el rango inicial"))
    y = int(input("Digite el número natural a ser el rango final"))
    n = int(input("Digite el número natural por el que serán separados los primos gemelos"))

    for x to y:
        i = EsPrimo(x)
        if i == True
            m = x + n

            if EsPrimo(m) == True

            primt (str(x)+"-"+str(m))
            
